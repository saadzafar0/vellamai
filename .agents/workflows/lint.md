# lint Workflow

Runs ESLint to check for stylistic, code quality, and formatting warnings or errors.

## Steps
1. Execute the ESLint checker in the workspace directory:
   ```bash
   pnpm lint
   ```
2. Correct any identified issues. All files must pass lint checks without errors.
