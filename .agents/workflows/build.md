# build Workflow

Runs the Next.js production build compiler to verify TypeScript type checking and asset bundling.

## Steps
1. Execute the Next.js compilation task in the workspace directory:
   ```bash
   pnpm build
   ```
2. Ensure the process exits with a code of `0` and generates the `.next/` directory.
