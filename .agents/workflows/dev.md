# dev Workflow

Starts the local development server for development and interactive testing.

## Steps
1. Execute the development command in the workspace directory:
   ```bash
   pnpm dev
   ```
2. Confirm the server boots correctly on `http://localhost:3000`.
